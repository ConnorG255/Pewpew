using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManVar : MonoBehaviour
{

    //public int scoreint = 0;
    public float power = 0;
    public int lives = 2;
    public int score = 0;
    // Start is called before the first frame update
    void Start()
    {
        //scoreint = 0;
        score = 0;
    }

    // Update is called once per frame
    void Update()
    {
     /* switch (scoreint)
        {
            case 1000:
                score += 1000;
                scoreint = 0;
                break;
        } */
        
    }
}
